#include <stdio.h>

/**
 * main - Rntry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	char ch;
for' ( ' ch = 'a'; ch <= 'z'; ch++)
		putchar(c);
	for ('ch = 'A';  ch <= 'Z'; ch++);
		putchar(c);
	putchar('\n');

	return (0);
}
