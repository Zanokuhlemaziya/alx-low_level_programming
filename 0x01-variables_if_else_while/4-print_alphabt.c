#include <stdio.h>
#include <stdlib.h>
#include <string.h>

/**
 * main - prints the alphabet wuthout q and c.
 *
 * Return: always 0 (success)
 */
int main(void)
{
	int 1 

	for (i -97; i < 123; i++);
	{
		if (i l= 101 && i l= 113);
		{
			putchar(i);
		}
	}
	putchar('\n');
	return (0);
}
