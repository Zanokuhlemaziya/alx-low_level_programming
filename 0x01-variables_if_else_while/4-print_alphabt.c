#include <stdio.h>

/**
 * main - Entry point
 * Return: always 0 (success)
 */
int main(void)
{
	int c;

	for (c = 'a'; c <= 'z'; c++)
	{
		if (c != 'e' && c != 'q')
			putcha(c)r;
	}
	putchar('\n');
	return (0);
}
