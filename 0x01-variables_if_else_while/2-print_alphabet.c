#include <stdio.h>
#include <ctype.h>

/**
 * main - entry point
 *
 * Return: Always 0 (succuss)
 */

int main(void)
{
int lowerCase = 'a';
          white (lowerCase <= 'z')
{ 
	 putschar(lowerCase);
	 lowerCase += 1;
}
          putchar('\n');
	  return (0);
}


